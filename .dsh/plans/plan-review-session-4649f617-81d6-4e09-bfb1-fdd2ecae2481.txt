计划待审
选中计划文本即可添加评论;修改意见请在 DSH 面板的计划审批输入框中输入并回车。

# 计划:生成「计划审批测试文档」

## 目标
在仓库新建 `docs/plan-approval-test.md`(markdown 测试用例文档),覆盖 DSH 插件的**计划审批(计划待审 card)**功能测试。

## 文档内容(基于代码 `src/webview/ui.ts` 2988-3026 行)
1. **概述**:计划审批卡片是什么、何时出现(`plan-review` 提问卡片)。
2. **前置条件**:`npx @deepseek-ai/dsh web` 启动、模型凭证、打开插件会话。
3. **触发方式**:`/plan` 进入计划模式 → 让 Agent 生成计划并请求审批。
4. **测试用例表**(编号 TC-PA-01…):
   - 卡片出现(📋 计划待审 + 计划 markdown 可滚动渲染 `item.detail`)。
   - 批准:`✅ 批准计划并开始执行` → `/api/respond` 提交批准选项 → 状态流转 running。
   - 继续修改:`✏️ 继续修改计划` → 提交非批准选项。
   - 计划模式芯片 📝 点击退出(`/plan`)、`plan/mode` 同步。
   - 多回合转场、会话列表"计划待审"状态行。
   - 异常:计划为空、服务器重连、快速连点。
5. **验收断言**:每个用例给"预期结果"与"通过条件"。
6. **记录区**:预留表格记录实际执行结果(本次多选测试已验证的链路可作参考附录)。

## 改动范围
- 仅新增 1 个文件 `docs/plan-approval-test.md`;不改任何源码/配置。
- README「Development」是否加一行指向该文档:默认为否,如你需要再加。

## 成功标准
- 文档存在、结构清晰、每条用例含 步骤 / 预期 / 通过条件,可直接照着测。
