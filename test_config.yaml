# Sample draft config file (YAML) for testing file creation
app: test-app
version: 1.0.0
enabled: true

server:
  host: 127.0.0.1
  port: 3080

features:
  - chat
  - goals
  - rollback

# updated with appended text
note: this line was appended as a simple test edit.
