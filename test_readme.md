# Test Readme

This is a sample markdown test file used to verify file creation.

## Features

- Supports headings
- Supports lists
- Supports *inline* **formatting** and `code`

## Example Code

```js
function hello(name) {
  return `Hello, ${name}!`;
}

console.log(hello("DSH"));
```

> Note: This file is a dummy draft for testing purposes only.

已添加的追加文本：updated with appended text.
